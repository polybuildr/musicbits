{
  "name": "musicbits",
  "version": "0.1.0",
  "description": "Node library to create music using baudio.",
  "main": "dist/index.js",
  "keywords": [
    "music"
  ],
  "author": "Vivek Ghaisas",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/polybuildr/musicbits.git"
  },
  "bugs": {
    "url": "https://github.com/polybuildr/musicbits/issues"
  },
  "homepage": "https://github.com/polybuildr/musicbits#readme",
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.3"
  },
  "scripts": {
    "build": "tsc --outDir dist/ --rootDir src/",
    "prepublish": "rm -rf dist/ && typings install && npm run build"
  },
  "dependencies": {
    "baudio": "^2.1.2"
  }
}
